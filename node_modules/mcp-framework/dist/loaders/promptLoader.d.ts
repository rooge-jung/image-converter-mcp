import { PromptProtocol } from "../prompts/BasePrompt.js";
export declare class PromptLoader {
    private readonly PROMPTS_DIR;
    private readonly EXCLUDED_FILES;
    constructor(basePath?: string);
    hasPrompts(): Promise<boolean>;
    private isPromptFile;
    private validatePrompt;
    loadPrompts(): Promise<PromptProtocol[]>;
}
