import { ResourceProtocol } from "../resources/BaseResource.js";
export declare class ResourceLoader {
    private readonly RESOURCES_DIR;
    private readonly EXCLUDED_FILES;
    constructor(basePath?: string);
    hasResources(): Promise<boolean>;
    private isResourceFile;
    private validateResource;
    loadResources(): Promise<ResourceProtocol[]>;
}
