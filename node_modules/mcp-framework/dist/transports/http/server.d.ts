import { IncomingMessage, ServerResponse } from 'node:http';
import { AbstractTransport } from '../base.js';
import { JSONRPCMessage } from '@modelcontextprotocol/sdk/types.js';
import { HttpStreamTransportConfig } from './types.js';
export declare class HttpStreamTransport extends AbstractTransport {
    readonly type = "http-stream";
    private _sdkTransport;
    private _isRunning;
    private _port;
    private _server?;
    private _endpoint;
    private _enableJsonResponse;
    private _sessionInitialized;
    private _pingInterval?;
    private _pingTimeouts;
    private _pingFrequency;
    private _pingTimeout;
    constructor(config?: HttpStreamTransportConfig);
    private createSdkTransport;
    start(): Promise<void>;
    private startPingInterval;
    private sendPing;
    private handlePingMessage;
    handleRequest(req: IncomingMessage, res: ServerResponse, body?: any): Promise<void>;
    send(message: JSONRPCMessage): Promise<void>;
    close(): Promise<void>;
    isRunning(): boolean;
}
